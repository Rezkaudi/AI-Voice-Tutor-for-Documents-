{
  "name": "ai-voice-tutor-for-documents",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bash -c 'npm run dev:back & backend=$!; npm run dev:front & frontend=$!; trap \"kill $backend $frontend 2>/dev/null\" INT TERM EXIT; wait -n $backend $frontend; status=$?; kill $backend $frontend 2>/dev/null; wait $backend $frontend 2>/dev/null; exit $status'",
    "dev:back": "PORT=5000 npm --prefix back-end run dev",
    "dev:front": "VITE_API_TARGET=http://localhost:5000 npm --prefix front-end run dev"
  }
}
